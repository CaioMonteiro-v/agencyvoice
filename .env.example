# Provedor: auto | elevenlabs | local
VOICE_PROVIDER=elevenlabs

# Secret ElevenLabs (começa com sk_). NÃO use o Key ID (hash hex).
# Nunca commit este valor — no Render, cole em Environment → ELEVENLABS_API_KEY
ELEVENLABS_API_KEY=

AI_URL=http://127.0.0.1:8000
PORT=3001
